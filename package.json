{
  "name": "video-curator-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm -w backend run dev & npm -w frontend run dev",
    "build": "npm -w shared run build && npm -w backend run build && npm -w frontend run build",
    "format": "echo 'Formatting can be added later'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
